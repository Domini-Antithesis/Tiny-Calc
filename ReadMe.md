#Tiny-Calc:

A tiny command-line calculator (add, subtract, multiply, divide).

# Usage: Run the calculator with python calculator.py from the project root for quick calculations.

python calculator.py